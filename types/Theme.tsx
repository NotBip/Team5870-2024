
/**
 * Theme type that defines the colors of the calendar.
 * The colors are stored as an array of strings.
 * The first color is the default color of an event.
 */
export type Theme = {
  name: string;
  colors: string[];
};